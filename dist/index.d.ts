export declare function pluralizeEn(num: number, words: string[]): string;
export declare function pluralizeRu(num: number, words: string[]): string;
export declare class Pluralize {
    static en(num: number, words: string[]): string;
    static ru(num: number, words: string[]): string;
}
